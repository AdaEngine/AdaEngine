import AdaEngine

class EventListenerScene {
    
    var disposeBag: Set<AnyCancellable> = []
    
    func makeScene() throws -> Scene {
        let scene = Scene()
        
        return scene
    }
}
