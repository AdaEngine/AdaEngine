# Agent Activity Glow — sample

Отключение эффекта: **Settings → General → Appearance → Agent activity glow**. Настройка применяется сразу во всех окнах, сохраняется между запусками и не останавливает агента. По умолчанию свечение включено.

Мягкая движущаяся волна по периметру окна: тонкая светлая кромка, цветное свечение внутрь окна и прозрачный центр. Оттенки получаются из одного `accent`, с небольшим сдвигом hue в обе стороны; для серого accent эффект остаётся нейтральным.

`agent-activity.glsl` — переносимое тело GLSL-шейдера, без привязки к vertex stage, uniform layout и API материалов. Нативная версия подключена в `Sources/AdaEditor/UI/Editor/EditorAgentActivityBackground.swift`; её ресурс — `Sources/AdaEditor/Assets/Shaders/agent_activity.glsl`.

Вызов во fragment stage:

```glsl
vec4 result = agentActivityGlow(
    position, size, elapsedSeconds * speed, accentRGB,
    activity, intensity, width, radius, hueSpread
);
```

Начальные параметры: `accentRGB = vec3(0.42, 0.36, 1.0)`, `activity = 1.0`, `intensity = 1.15`, `width = 42.0`, `radius = 14.0`, `speed = 1.0`.

- `position`, `size`, `width`, `radius`: логические пиксели, начало координат сверху слева; `size` больше 2 px по обеим осям, `width >= 1`.
- `elapsedSeconds`: монотонное время относительно запуска эффекта; `speed` в пределах 0–2 для sample.
- `accentRGB`: RGB 0–1 в цветовом пространстве целевого render target. При переносе в linear pipeline преобразуйте входной цвет соответствующим образом.
- `activity`: 0–1, плавное появление и затухание. Для ожидания можно использовать интенсивность 0.6, для внесения правок — 1.15. Примерные значения, не измеренные показатели работы агента.
- Результат premultiplied RGBA: blend `ONE, ONE_MINUS_SRC_ALPHA`. Не умножайте RGB на alpha повторно.
- Рисуйте отдельным overlay поверх окна, без hit testing. Не меняйте layout содержимого. Радиус должен совпадать с окном; для прямых углов передайте 0.
- При Reduce Motion заморозьте время и переключайте цвет сразу; сохраняйте текстовый статус. Также приостанавливайте время и отрисовку у невидимого окна.

## Состояния

В демо состояния переключаются списком внизу окна. Это симуляция, не реальные события агента.

| Состояние | Цвет | Движение волн | `hueSpread` |
| --- | --- | --- | --- |
| `working` — работает | Настраиваемый accent | Обычная скорость | 0.065 |
| `completed` — завершил | Зелёный `#34D27B` | Плавно останавливается, свечение сохраняется | 0.012 |
| `needsInput` — tool approval или любое действие пользователя | Жёлтый `#F5C842` | 0.32 от обычной скорости | 0.012 |
| `failed` — ошибка | Красный `#F06464` | 0.15 от обычной скорости | 0.012 |

Состояние разрешается на стороне приложения: передавайте соответствующий RGB как `accent`. Пользовательский accent сохраняется отдельно и снова используется при возвращении в `working`. Цвета статусов не зависят от него. Узкий `hueSpread` сохраняет узнаваемость зелёного, жёлтого и красного по всему периметру.

Цвет и скорость переходят к новым значениям через `1 - exp(-deltaSeconds * 14)` с привязкой ко времени; почти весь переход занимает 300–400 ms. Продолжайте накапливать фазу времени, меняя её скорость, чтобы волны не прыгали при смене состояния. В `completed` остановите render callbacks после окончания перехода; последнее зелёное изображение остаётся видимым до следующего состояния или явного скрытия overlay. Для скрытия отдельно затушите `activity` до нуля.

Статус дублируется текстом и значком. Для подключения к агенту ошибка имеет приоритет над ожиданием пользователя; ожидание — над работой. `completed` выбирается только после подтверждённого успешного завершения без ожидающих approval/действий. Наличие внесённых правок само по себе не означает завершение.

Вызов без `hueSpread` остаётся совместимым и использует 0.065. Для новых состояний используйте расширенную сигнатуру.

GLSL-тело используется в интерактивном WebGL sample. Нативная версия использует `UIShaderMaterial`, передаёт размер фонового слоя и обновляет uniforms без перестроения дерева редактора. Рабочий accent берётся из `theme.editorColors.blue`.

В редакторе `EditorAgentActivityOverlay` рисует эффект поверх интерфейса по внешнему периметру всего окна, без отступов тулбара, футера и боковых полос. Он игнорирует safe area; центр прозрачен, клики проходят сквозь слой. Собственные фоны панелей сохранены, дополнительные защитные подложки удалены. `EditorSafeAreaBackground` остаётся обычным фоном. Состояние берётся из последней операции именно этого окна: `needsAttention` означает approval или другое явно зарегистрированное ожидание пользователя; отмена скрывает эффект, ошибка соединения отображается красным. Текст произвольных сообщений агента не используется для угадывания состояния.

Важно: `@Uniform` привязывает весь блок `EditorAgentGlowMaterial` к структуре `EditorAgentGlowParameters`, а не отдельные `u_Color`/`u_Geometry`/`u_Style`. Система материалов обновляет целый отражённый uniform-буфер. Тест Metal считывает фактически загруженные через `CustomMaterial` значения, прежде чем рисовать ими.
